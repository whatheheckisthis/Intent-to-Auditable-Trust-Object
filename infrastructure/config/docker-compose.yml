services:
  enclave:
    image: enclavenetworks/enclave:latest
    container_name: enclave
    restart: unless-stopped
    cap_add: ["NET_ADMIN"]
    devices:
      - /dev/net/tun:/dev/net/tun
    environment:
      ENCLAVE_ENROLMENT_KEY: ${ENCLAVE_ENROLMENT_KEY:?set in .env}
    volumes:
      - enclave-profiles:/etc/enclave/profiles
    networks: [enclave_net]

  xdp-bridge:
    build:
      context: ./ebpf
      dockerfile: netflow-sidecar/Dockerfile
    container_name: xdp-bridge
    restart: unless-stopped
    privileged: true
    network_mode: service:enclave
    depends_on: [enclave]
    environment:
      XDP_INTERFACE: ${XDP_INTERFACE:-eth0}
      XDP_MODE: ${XDP_MODE:-drv}
      PERF_EVENT_MAP: /sys/fs/bpf/v7_perf_events
      PERF_OUTPUT_SYMBOL: bpf_perf_event_output
    volumes:
      - /sys/kernel/btf:/sys/kernel/btf:ro
      - /sys/fs/bpf:/sys/fs/bpf
      - /lib/modules:/lib/modules:ro

  qemu-sve:
    image: multiarch/qemu-user-static:latest
    container_name: qemu-sve
    restart: unless-stopped
    network_mode: service:enclave
    depends_on: [xdp-bridge, softhsm, ipfs]
    entrypoint: ["/bin/sh", "-lc"]
    command: >
      "qemu-aarch64-static
      -cpu neoverse-v2,sve=on,sve2=on
      -L /usr/aarch64-linux-gnu
      /opt/dispatcher/dispatcher_aarch64
      --perf-map /sys/fs/bpf/v7_perf_events
      --mask-routine /opt/dispatcher/mask_sve2.s
      --pkcs11-module ${PKCS11_MODULE_PATH:-/usr/lib/softhsm/libsofthsm2.so}
      --pkcs11-pin ${PKCS11_PIN:-1234}
      --key-label ${HSM_KEY_LABEL:-ato-ed25519}
      --prometheus-bind 0.0.0.0:9600"
    environment:
      IPFS_API_HOST: ${IPFS_API_HOST:-127.0.0.1}
      IPFS_API_PORT: ${IPFS_API_PORT:-5001}
      HSM_KEY_ID: ${HSM_KEY_ID:-ato-ed25519}
    volumes:
      - /sys/fs/bpf:/sys/fs/bpf
      - ./mask_sve2.s:/opt/dispatcher/mask_sve2.s:ro
      - ./dispatcher.c:/opt/dispatcher/dispatcher.c:ro
      - ./bin/dispatcher_aarch64:/opt/dispatcher/dispatcher_aarch64:ro

  softhsm:
    image: ghcr.io/softhsm/softhsm2:latest
    container_name: softhsm
    restart: unless-stopped
    environment:
      SOFTHSM2_CONF: /etc/softhsm2.conf
    volumes:
      - softhsm-tokens:/var/lib/softhsm/tokens


  ipfs:
    image: ipfs/kubo:v0.31.0
    container_name: ipfs
    restart: unless-stopped
    network_mode: service:enclave
    depends_on: [enclave]
    environment:
      IPFS_PATH: /data/ipfs
      LIBP2P_FORCE_PNET: "1"
      ENCLAVE_VIF_ADDR: ${ENCLAVE_VIF_ADDR:-100.96.0.10}
      IPFS_API_PORT: ${IPFS_API_PORT:-5001}
      IPFS_SWARM_PORT: ${IPFS_SWARM_PORT:-4001}
    command: >
      /bin/sh -lc '
      if [ ! -f /data/ipfs/config ]; then ipfs init --profile=server; fi;
      ipfs bootstrap rm --all;
      ipfs config Addresses.API /ip4/${ENCLAVE_VIF_ADDR}/tcp/${IPFS_API_PORT};
      ipfs config Addresses.Gateway /ip4/${ENCLAVE_VIF_ADDR}/tcp/8080;
      ipfs config --json Addresses.Swarm ["/ip4/${ENCLAVE_VIF_ADDR}/tcp/${IPFS_SWARM_PORT}"];
      ipfs config Routing.Type none;
      ipfs daemon --migrate=true --agent-version-suffix=ato-enclave'
    volumes:
      - ipfs-data:/data/ipfs
      - ./ipfs/swarm.key:/data/ipfs/swarm.key:ro

  prometheus:
    image: prom/prometheus:v2.53.1
    container_name: prometheus
    restart: unless-stopped
    network_mode: service:enclave
    depends_on: [qemu-sve]
    command:
      - --config.file=/etc/prometheus/prometheus.yml
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus

  db:
    image: mariadb:11.4
    container_name: mariadb
    restart: unless-stopped
    environment:
      MARIADB_DATABASE: ${DRUPAL_DB_NAME:-drupal}
      MARIADB_USER: ${DRUPAL_DB_USER:-drupal}
      MARIADB_PASSWORD: ${DRUPAL_DB_PASSWORD:?set in .env}
      MARIADB_ROOT_PASSWORD: ${MARIADB_ROOT_PASSWORD:?set in .env}
    networks: [app_net]
    volumes:
      - mariadb-data:/var/lib/mysql

  drupal:
    image: drupal:10-apache
    container_name: drupal
    restart: unless-stopped
    depends_on: [db]
    environment:
      DRUPAL_DB_HOST: db
      DRUPAL_DB_NAME: ${DRUPAL_DB_NAME:-drupal}
      DRUPAL_DB_USER: ${DRUPAL_DB_USER:-drupal}
      DRUPAL_DB_PASSWORD: ${DRUPAL_DB_PASSWORD:?set in .env}
    networks: [app_net]

  nginx:
    image: nginx:1.27-alpine
    container_name: app-gateway
    restart: unless-stopped
    depends_on: [drupal]
    ports:
      - "8080:80"
    networks: [app_net]
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro

volumes:
  enclave-profiles:
  softhsm-tokens:
  prometheus-data:
  ipfs-data:
  mariadb-data:

networks:
  enclave_net:
    driver: bridge
  app_net:
    driver: bridge
    internal: true
