# 18.X Kernel Beginner Notes (Weeks 1–6)

These notes guide you from the very foundations to beginner-level execution of the 18.X Kernel.

---

## Week 1: Linux & Terminal Basics
- Learn how to use the Linux terminal.
- Commands: `pwd`, `ls`, `cd`, `mkdir`, `rm`, `cp`, `mv`.
- Understand the file system structure (`/home`, `/usr`, `/etc`).
- Practice installing and updating packages:
  ```bash
  sudo apt update && sudo apt upgrade -y
